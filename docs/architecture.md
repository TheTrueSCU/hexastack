# Monorepo Architecture & Principles

> Hexastack enforces clean, decoupled hexagonal architectures (Ports & Adapters) through automated linting contracts and strict verification layers.

---

## 1. The Hexagonal Golden Rules

1. **Pure Domain Center**:
   - The domain layer contains zero dependencies on databases, ORMs, HTTP frameworks, or external infrastructure.
   - All domain errors subclass `HexastackError`.
2. **Ports as Inversion Boundaries**:
   - `Driving Ports` (inbound) expose application interfaces (e.g. `CommandBusPort`, `QueryBusPort`).
   - `Driven Ports` (outbound) define infrastructure contracts (e.g. `RepositoryPort`, `LoggerPort`, `OutboxPort`).
3. **Adapters Implement Ports**:
   - Adapters translate between external protocols (FastAPI, Typer CLI, gRPC, LiteLLM) and internal domain/port models.

```mermaid
flowchart LR
    subgraph Driving ["Inbound Driving Adapters"]
        REST[FastAPI HTTP]
        CLI[Typer CLI]
        MCP[MCP AI Tool]
    end

    subgraph Ports ["Application Ports"]
        CBUS[CommandBusPort]
        QBUS[QueryBusPort]
    end

    subgraph Core ["Pure Domain"]
        ENTITY[Entities & Aggregates]
        LOGIC[Business Invariants]
    end

    subgraph Driven ["Outbound Driven Adapters"]
        SQL[SQLAlchemy DB]
        OTEL[OTel Tracing]
        OUTBOX[CloudEvents Outbox]
        NATS["NATS JetStream\n(NatsJetStreamEventBusAdapter)"]
    end

    REST --> CBUS
    CLI --> CBUS
    MCP --> QBUS

    CBUS --> ENTITY
    QBUS --> ENTITY

    ENTITY --> SQL
    ENTITY --> OTEL
    ENTITY --> OUTBOX
    ENTITY --> NATS
```

---

## 2. Monorepo Quality Gates & Enforcement

- **`import-linter`**: Enforces strict unidirectional dependencies across packages and layers.
- **`complexipy`**: Cognitive complexity limit per function ($\le 15$).
- **`ty check`**: Strict type checking with no untyped definitions.
- **`hypothesis`**: Property-based fuzzing on core pipelines and routing algorithms.
- **`mutmut`**: Mutation testing ensuring resilient assertions.

---

## 3. Dependency Diagrams

> Generated by [`pydeps-generate`](../scripts/pydeps/generate.py). Diagrams show internal module structure and third-party dependencies. Regenerate with `uv run pydeps-generate`; CI enforces freshness.

### Monorepo Overview

![Hexastack monorepo package dependency overview](assets/pydeps/hexastack_packages.svg)

### Per-Package Diagrams

| Package | Diagram |
|---|---|
| `hexastack` | ![hexastack](assets/pydeps/hexastack.svg) |
| `hexastack_ai` | ![hexastack_ai](assets/pydeps/hexastack_ai.svg) |
| `hexastack_auth` | ![hexastack_auth](assets/pydeps/hexastack_auth.svg) |
| `hexastack_cli` | ![hexastack_cli](assets/pydeps/hexastack_cli.svg) |
| `hexastack_core` | ![hexastack_core](assets/pydeps/hexastack_core.svg) |
| `hexastack_cqrs` | ![hexastack_cqrs](assets/pydeps/hexastack_cqrs.svg) |
| `hexastack_db` | ![hexastack_db](assets/pydeps/hexastack_db.svg) |
| `hexastack_events` | ![hexastack_events](assets/pydeps/hexastack_events.svg) |
| `hexastack_fastapi` | ![hexastack_fastapi](assets/pydeps/hexastack_fastapi.svg) |
| `hexastack_graphql` | ![hexastack_graphql](assets/pydeps/hexastack_graphql.svg) |
| `hexastack_grpc` | ![hexastack_grpc](assets/pydeps/hexastack_grpc.svg) |
| `hexastack_logging` | ![hexastack_logging](assets/pydeps/hexastack_logging.svg) |
| `hexastack_mcp` | ![hexastack_mcp](assets/pydeps/hexastack_mcp.svg) |
| `hexastack_otel` | ![hexastack_otel](assets/pydeps/hexastack_otel.svg) |
