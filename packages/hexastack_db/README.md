# Hexastack Database (`hexastack-db`)

SQLAlchemy database adapter, generic Repository, and Unit of Work for Hexastack.

## Installation

```bash
# SQLite support
pip install hexastack-db[sqlite]

# PostgreSQL support
pip install hexastack-db[postgresql]

# All database drivers
pip install hexastack-db[all]
```
